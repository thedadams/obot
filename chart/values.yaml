# replicaCount -- The number of Obot server instances to run.
# When greater than one, the chart configures replicas to peer tunnel connections through the Obot Service.
replicaCount: 1

tunnelPeer:
  # tunnelPeer.existingSecret -- An existing Secret containing OBOT_SERVER_TUNNEL_PEER_TOKEN. Use this or token for stable GitOps rendering. The chart creates and preserves a token when both are empty.
  existingSecret: ""
  # tunnelPeer.token -- The shared peer token stored in the chart-managed Secret. Set this to a stable secret value when manifests are rendered without live-cluster lookup (for example, with a GitOps renderer).
  token: ""

dev:
  # dev.useEmbeddedDb -- For development/testing use only, enables the use of an postgres database embedded in the obot container. Do not use in production.
  useEmbeddedDb: false

image:
  # image.repository -- The name of the docker repository for Obot. `ghcr.io/obot-platform/obot` for open-source or `ghcr.io/obot-platform/obot-enterprise` for enterprise.
  # Please note that for enterprise you will need to set an `imagePullSecret`
  repository: ghcr.io/obot-platform/obot
  # image.tag -- The docker tag to pull for obot. If blank, will default to the chart appVersion
  tag: ""
  # image.pullPolicy -- Kubernetes image pullPolicy to use for Obot
  pullPolicy: IfNotPresent

# imagePullSecrets -- Kubernetes secrets used to pull the Obot server image.
# For private MCP server images, see mcpImagePullSecrets below.
# Expects a list of secret objects
# `imagePullSecrets:[{"name": "fooBar"}]`
imagePullSecrets: []

# updateStrategy -- Configures what update strategy to use for the deployment (Recreate or RollingUpdate)
updateStrategy: "RollingUpdate"

# Additional labels that should be applied to all resources
additionalLabels: {}
  # app: obot

# -- Extra pod annotations to add.
podAnnotations: {}

# topologySpreadConstraints -- Configurable topologySpreadConstraints for Obot pods
topologySpreadConstraints: {}

service:
  # service.type -- Type of Kubernetes service to create
  type: ClusterIP
  # service.port -- Port for the Kubernetes service to expose
  port: 80
  # service.annotations -- Extra annotations to add to service object
  annotations: {}
  # service.spec -- Any extra fields to add to the service object spec
  spec: {}

ingress:
  # ingress.enabled -- Enables ingress creation for Obot.
  enabled: false
  # ingress.annotations -- Configure annotations to add to the ingress object
  annotations: {}
  # kubernetes.io/ingress.class: nginx
  # kubernetes.io/tls-acme: "true"
  # ingress.className -- Configures a preexisting ingress class to use.
  className: ~
  # className: obot
  paths:
    - path: /
      pathType: Prefix
  # ingress.extraPaths -- Define complete path objects, will be inserted before regular paths. Can be useful for things like ALB Ingress Controller actions
  extraPaths: []
  # ingress.hosts -- List of hostnames to configure the ingress with. Required when ingress.enabled is true.
  hosts: []
  # - chart-example.local
  # ingress.tls -- List of secrets used to configure TLS for the ingress.
  tls: []
  #  - secretName: chart-example-tls
  #    hosts:
  #      - chart-example.local

# secret - Sensitive configuration variables for the Obot deployment. When secret.existingSecret is not set, these values are converted to a Secret and used via envFrom. Non-sensitive configuration belongs under config.
secret:
  # secret.existingSecret -- The name of an existing Secret to use instead of creating one. It only needs to contain the sensitive keys configured below; non-sensitive chart-derived configuration is still supplied by the chart ConfigMap.
  existingSecret: ""

  # secret.AWS_ACCESS_KEY_ID -- An AWS access key with permissions for AWS KMS, used for encryption
  AWS_ACCESS_KEY_ID: ""
  # secret.AWS_SECRET_ACCESS_KEY -- An AWS secret access key with permissions for AWS KMS, used for encryption
  AWS_SECRET_ACCESS_KEY: ""

  # secret.GITHUB_AUTH_TOKEN -- A GitHub PAT, used to authenticate catalog and tool pulls to avoid GitHub rate limiting
  GITHUB_AUTH_TOKEN: ""
  # secret.OBOT_SERVER_ENCRYPTION_KEY -- The key to use for encryption. Only used if config.OBOT_SERVER_ENCRYPTION_PROVIDER is 'custom'. A key can be generated with `openssl rand -base64 32`
  OBOT_SERVER_ENCRYPTION_KEY: ""
  # secret.OBOT_BOOTSTRAP_TOKEN -- Sets a bootstrap token. If the chart is managing the Secret, one will be autogenerated (and preserved across upgrades) when this is left empty.
  OBOT_BOOTSTRAP_TOKEN: ""
  # secret.OBOT_SERVER_DSN -- The DSN for your database. For example: postgres://<username>:<password>@<hostname>/<db_name>
  OBOT_SERVER_DSN: ""
  # secret.OPENAI_API_KEY -- An OpenAI API Key used to configure access to OpenAI models, which are the default in Obot.
  OPENAI_API_KEY: ""
  # secret.ANTHROPIC_API_KEY -- An Anthropic API Key used to configure access to Anthropic models, which can be used as the default in Obot.
  ANTHROPIC_API_KEY: ""

  # secret.OBOT_SERVER_OTEL_BEARER_TOKEN -- The bearer token for authentication with OpenTelemetry
  OBOT_SERVER_OTEL_BEARER_TOKEN: ""

  # secret.OBOT_ARTIFACT_S3_ACCESS_KEY_ID -- Access key ID for published workflow storage when explicit S3-compatible credentials are required.
  OBOT_ARTIFACT_S3_ACCESS_KEY_ID: ""
  # secret.OBOT_ARTIFACT_S3_SECRET_ACCESS_KEY -- Secret access key for published workflow storage when explicit S3-compatible credentials are required.
  OBOT_ARTIFACT_S3_SECRET_ACCESS_KEY: ""
  # secret.OBOT_ARTIFACT_GCS_SERVICE_ACCOUNT_JSON -- Inline GCS service account JSON for published workflow storage. If empty, Application Default Credentials are used.
  OBOT_ARTIFACT_GCS_SERVICE_ACCOUNT_JSON: ""
  # secret.OBOT_ARTIFACT_AZURE_CLIENT_SECRET -- Azure client secret for published workflow storage when using explicit Azure credentials.
  OBOT_ARTIFACT_AZURE_CLIENT_SECRET: ""

  # secret.OBOT_SERVER_LICENSE_KEY -- A license key for Obot Enterprise. If set here, it cannot be set in the UI.
  OBOT_SERVER_LICENSE_KEY: ""

# config - Non-sensitive configuration variables for the Obot deployment. These values are converted to a ConfigMap and used via envFrom.
config:
  # config.AWS_REGION -- An AWS region, used to access AWS KMS
  AWS_REGION: ""
  # config.OBOT_GCP_KMS_KEY_URI -- The URI of a Google Cloud KMS key, used for encryption
  OBOT_GCP_KMS_KEY_URI: ""

  # config.NAH_THREADINESS -- Advanced - sets the number of concurrent threads that can run in the Obot controller
  NAH_THREADINESS: "10000"
  # config.KINM_DB_CONNECTIONS -- Advanced - the number of connections in the database pool for kinm, both open and idle connections
  KINM_DB_CONNECTIONS: "5"
  # config.KINM_DB_MAX_IDLE_CONNECTIONS -- Advanced - the number of idle connections in the database pool for kinm, overrides KINM_DB_CONNECTIONS
  KINM_DB_MAX_IDLE_CONNECTIONS: "2"
  # config.KINM_DB_MAX_CONNECTIONS -- Advanced - the number of open connections in the database pool for kinm, overrides KINM_DB_CONNECTIONS
  KINM_DB_MAX_CONNECTIONS: "5"
  # config.KINM_DB_MAX_CONNECTION_LIFETIME_SECONDS -- Advanced - the maximum lifetime of a connection in the database pool for kinm in seconds
  KINM_DB_MAX_CONNECTION_LIFETIME_SECONDS: "180"
  # config.OBOT_AUTH_PROVIDER_POSTGRES_MAX_IDLE_CONNECTIONS -- Advanced - the maximum number of idle connections in the database pool for the auth provider
  OBOT_AUTH_PROVIDER_POSTGRES_MAX_IDLE_CONNECTIONS: "2"
  # config.OBOT_AUTH_PROVIDER_POSTGRES_MAX_CONNECTIONS -- Advanced - the maximum number of connections in the database pool for the auth provider
  OBOT_AUTH_PROVIDER_POSTGRES_MAX_CONNECTIONS: "5"
  # config.OBOT_AUTH_PROVIDER_POSTGRES_CONNECTION_LIFETIME_SECONDS -- Advanced - the maximum lifetime of a connection in the database pool for the auth provider in seconds
  OBOT_AUTH_PROVIDER_POSTGRES_CONNECTION_LIFETIME_SECONDS: "180"



  # config.OBOT_SERVER_ENABLE_AUTHENTICATION -- Enables authentication for Obot
  OBOT_SERVER_ENABLE_AUTHENTICATION: false
  # config.OBOT_SERVER_ENABLE_REGISTRY_AUTH -- Enables authentication for the MCP registry API. When false (default), registry is accessible without authentication and returns only default catalog items with wildcard access control rules.
  OBOT_SERVER_ENABLE_REGISTRY_AUTH: false
  # config.OBOT_ENABLE_AGENTS -- Controls Obot Agent features. Leave empty (default) to disable agents for new deployments while grandfathering in (keeping enabled) existing deployments that already have agents. Set to "true" to force-enable, or "false" to force-disable, regardless of grandfathering.
  OBOT_ENABLE_AGENTS: ""
  # Hosted agent sandboxes follow OBOT_SERVER_MCPRUNTIME_BACKEND: they run on Kubernetes when MCP servers do, in the namespace configured under mcpNamespace. There is deliberately no separate backend setting, because a deployment always wants the two to match.
  # config.OBOT_SERVER_HOSTED_AGENTS_STORAGE_CLASS_NAME -- StorageClass for hosted agent pool volumes. Each pool gets one ReadWriteOnce volume that every sandbox in the pool mounts, so the StorageClass must use volumeBindingMode: WaitForFirstConsumer -- that is what confines a pool to a single node. Leaving this empty uses the cluster default StorageClass, which commonly binds immediately and can leave a pool's sandboxes unschedulable.
  OBOT_SERVER_HOSTED_AGENTS_STORAGE_CLASS_NAME: ""
  # config.OBOT_SERVER_HOSTED_AGENTS_RUNTIME_CLASS_NAME -- RuntimeClass for hosted agent deployments.
  OBOT_SERVER_HOSTED_AGENTS_RUNTIME_CLASS_NAME: ""
  # config.OBOT_SERVER_HOSTED_AGENTS_CLEANUP_IMAGE -- Image used to erase a deleted sandbox's directory from its pool volume. Needs a shell and coreutils. Defaults to busybox:1.36. Set this when the cluster cannot pull from Docker Hub.
  OBOT_SERVER_HOSTED_AGENTS_CLEANUP_IMAGE: ""
  # config.OBOT_HOSTED_AGENTS_IMAGE_PULL_POLICY -- Pull policy for hosted agent sandbox images (Always, IfNotPresent, Never). Empty means Always, which refuses an image preloaded onto the node rather than published to a registry; use IfNotPresent for air-gapped installs.
  OBOT_HOSTED_AGENTS_IMAGE_PULL_POLICY: ""
  # config.OBOT_SERVER_UNAUTHENTICATED_RATE_LIMIT -- Rate limit for unauthenticated requests in requests per second. Tracked by source IP address. Defaults to 100.
  OBOT_SERVER_UNAUTHENTICATED_RATE_LIMIT: ""
  # config.OBOT_SERVER_AUTHENTICATED_RATE_LIMIT -- Rate limit for authenticated non-admin requests in requests per second. Tracked by user ID. Admin users are exempt. Defaults to 200.
  OBOT_SERVER_AUTHENTICATED_RATE_LIMIT: ""
  # config.OBOT_SERVER_ENCRYPTION_PROVIDER -- Configures an encryption provider for credentials in Obot
  OBOT_SERVER_ENCRYPTION_PROVIDER: "" # "aws", "gcp", "azure", "custom"
  # config.OBOT_SERVER_ENCRYPTION_CONFIG_FILE -- The path to a file containing the encryption configuration. Only used if config.OBOT_SERVER_ENCRYPTION_PROVIDER is 'custom'
  OBOT_SERVER_ENCRYPTION_CONFIG_FILE: ""
  # config.OBOT_SERVER_AUTH_OWNER_EMAILS -- A comma separated list of email addresses that will have the Owner role in Obot.
  OBOT_SERVER_AUTH_OWNER_EMAILS: ""
  # config.OBOT_SERVER_AUTH_ADMIN_EMAILS -- A comma separated list of email addresses that will have the Admin role in Obot.
  OBOT_SERVER_AUTH_ADMIN_EMAILS: ""
  # config.OBOT_SERVER_HOSTNAME -- The hostname of your Obot instance, including protocol
  OBOT_SERVER_HOSTNAME: ""

  # config.OBOT_SERVER_DAILY_USER_INPUT_TOKEN_LIMIT -- The maximum number of prompt/input tokens allowed per user per day. Set to a negative value to disable this limit. Defaults to 10000000.
  OBOT_SERVER_DAILY_USER_INPUT_TOKEN_LIMIT: ""
  # config.OBOT_SERVER_DAILY_USER_OUTPUT_TOKEN_LIMIT -- The maximum number of completion/output tokens allowed per user per day. Set to a negative value to disable this limit. Defaults to 100000.
  OBOT_SERVER_DAILY_USER_OUTPUT_TOKEN_LIMIT: ""

  # config.OBOT_SERVER_IDLE_AGENT_SHUTDOWN_HOURS -- The interval in hours to check for idle agents and shut them down. Set to -1 to disable. Defaults to 72.
  OBOT_SERVER_IDLE_AGENT_SHUTDOWN_HOURS: ""
  # config.OBOT_SERVER_SINGLE_USER_IDLE_SERVER_SHUTDOWN_HOURS -- The interval in hours to check for idle single-user MCP servers and shut them down. Set to -1 to disable. Defaults to 24.
  OBOT_SERVER_SINGLE_USER_IDLE_SERVER_SHUTDOWN_HOURS: ""
  # config.OBOT_SERVER_MULTI_USER_IDLE_SERVER_SHUTDOWN_HOURS -- The interval in hours to check for idle multi-user MCP servers and shut them down. Set to -1 to disable. Defaults to 168.
  OBOT_SERVER_MULTI_USER_IDLE_SERVER_SHUTDOWN_HOURS: ""

  # config.OBOT_SERVER_AUDIT_LOGS_MODE -- Configures the storage backend for audit logs in Obot. Can be 'off', 'disk', or 's3'
  OBOT_SERVER_AUDIT_LOGS_MODE: "off"
  # config.OBOT_SERVER_AUDIT_LOGS_STORE_S3BUCKET -- The name of the S3 bucket to store audit logs in. Only used if config.OBOT_SERVER_AUDIT_LOGS_MODE is 's3'
  OBOT_SERVER_AUDIT_LOGS_STORE_S3BUCKET: ""
  # config.OBOT_SERVER_AUDIT_LOGS_STORE_S3ENDPOINT -- If config.OBOT_SERVER_AUDIT_LOGS_MODE is 's3' and you are not using AWS S3, this needs to be set to the S3 api endpoint of your provider.
  OBOT_SERVER_AUDIT_LOGS_STORE_S3ENDPOINT: ""
  # config.OBOT_SERVER_AUDIT_LOGS_COMPRESS_FILE -- Whether to compress audit log files
  OBOT_SERVER_AUDIT_LOGS_COMPRESS_FILE: true
  # config.OBOT_SERVER_AUDIT_LOGS_USE_PATH_STYLE -- Whether to use path style for S3
  OBOT_SERVER_AUDIT_LOGS_USE_PATH_STYLE: false

  # config.OBOT_SERVER_MCPBASE_IMAGE -- Deploy MCP servers in the cluster using this base image.
  OBOT_SERVER_MCPBASE_IMAGE: ""
  # config.OBOT_SERVER_NANOBOT_AGENT_IMAGE -- Container image for the Nanobot agent MCP server.
  OBOT_SERVER_NANOBOT_AGENT_IMAGE: ""
  # config.OBOT_SERVER_MCPHTTPWEBHOOK_BASE_IMAGE -- Deploy MCP HTTP webhook servers in the cluster using this base image.
  OBOT_SERVER_MCPHTTPWEBHOOK_BASE_IMAGE: ""
  # config.OBOT_SERVER_MCPCLUSTER_DOMAIN -- The cluster domain to use for MCP services. Defaults to cluster.local. Only matters if the above image is set.
  OBOT_SERVER_MCPCLUSTER_DOMAIN: ""
  # config.OBOT_SERVER_DISALLOW_LOCALHOST_MCP -- Disallow MCP servers that try to connect to localhost. Defaults to true.
  OBOT_SERVER_DISALLOW_LOCALHOST_MCP: ""
  # config.OBOT_SERVER_DISALLOW_PRIVATE_IPMCP -- Disallow MCP servers that resolve to private IP addresses. Defaults to true.
  OBOT_SERVER_DISALLOW_PRIVATE_IPMCP: ""
  # config.OBOT_SERVER_DISALLOW_LINK_LOCAL_MCP -- Disallow MCP servers that resolve to link-local addresses. Defaults to true.
  OBOT_SERVER_DISALLOW_LINK_LOCAL_MCP: ""
  # config.OBOT_SERVER_MCPRUNTIME_BACKEND -- The runtime backend to use for MCP servers. Can be 'docker' or 'kubernetes'. Defaults to 'docker'. Setting this to 'kubernetes' will also create the necessary service account, role and rolebinding.
  OBOT_SERVER_MCPRUNTIME_BACKEND: "kubernetes"
  # config.OBOT_SERVER_MCPSECRET_BINDING_ALLOWED_LABEL -- Kubernetes Secret label key required for admin UI secret-binding lookup and runtime secret-binding resolution. Empty uses the server default: obot.obot.ai/allow-secret-binding.
  OBOT_SERVER_MCPSECRET_BINDING_ALLOWED_LABEL: ""

  # Network policy provider Helm chart configuration.
  # config.OBOT_SERVER_MCPNETWORK_POLICY_PROVIDER_CHART_REPO -- Helm repository URL for the network policy provider chart. Used when config.OBOT_SERVER_MCPNETWORK_POLICY_PROVIDER_CHART_NAME is set.
  OBOT_SERVER_MCPNETWORK_POLICY_PROVIDER_CHART_REPO: ""
  # config.OBOT_SERVER_MCPNETWORK_POLICY_PROVIDER_CHART_NAME -- Helm chart name for the network policy provider chart. Setting this enables network policy integration.
  OBOT_SERVER_MCPNETWORK_POLICY_PROVIDER_CHART_NAME: ""
  # config.OBOT_SERVER_MCPNETWORK_POLICY_PROVIDER_CHART_VERSION -- Helm chart version for the network policy provider chart.
  OBOT_SERVER_MCPNETWORK_POLICY_PROVIDER_CHART_VERSION: ""
  # config.OBOT_SERVER_MCPNETWORK_POLICY_PROVIDER_CHART_PATH -- Local filesystem path to the network policy provider chart. Setting this enables network policy integration.
  OBOT_SERVER_MCPNETWORK_POLICY_PROVIDER_CHART_PATH: ""
  # config.OBOT_SERVER_MCPNETWORK_POLICY_PROVIDER_VALUES -- YAML or JSON values blob merged into the network policy provider chart values.
  OBOT_SERVER_MCPNETWORK_POLICY_PROVIDER_VALUES: ""
  # config.OBOT_SERVER_MCPDEFAULT_DENY_ALL_EGRESS -- Default new MCP servers to allow all egress or deny all egress when a network policy provider is configured. Set to false to default to allow all egress, or true to default to deny all egress. Defaults to false.
  OBOT_SERVER_MCPDEFAULT_DENY_ALL_EGRESS: ""

  # config.OBOT_SERVER_MCPAUDIT_LOG_PERSIST_INTERVAL_SECONDS -- The interval in seconds to persist MCP audit logs to the database. Defaults to 5 seconds.
  OBOT_SERVER_MCPAUDIT_LOG_PERSIST_INTERVAL_SECONDS: ""
  # config.OBOT_SERVER_MCPAUDIT_LOGS_PERSIST_BATCH_SIZE -- The batch size to use when persisting MCP audit logs to the database. Defaults to 1000
  OBOT_SERVER_MCPAUDIT_LOGS_PERSIST_BATCH_SIZE: ""

  # config.OBOT_SERVER_DISABLE_UPDATE_CHECK -- Disable the Obot server update check. Defaults to false.
  OBOT_SERVER_DISABLE_UPDATE_CHECK: ""

  # config.OBOT_SERVER_HIDE_K8S_DETAILS -- Hide Kubernetes configuration details such as the Server Scheduling page from the UI. Defaults to false.
  OBOT_SERVER_HIDE_K8S_DETAILS: false

  # config.OBOT_SERVER_MCPOAUTH_CLIENT_EXPIRATION -- The expiration time for dynamically registered MCP OAuth clients. Must be a valid duration string and may include days, hours, or minutes. Defaults to 30d.
  OBOT_SERVER_MCPOAUTH_CLIENT_EXPIRATION: ""

  # config.OBOT_SERVER_FORCE_DYNAMIC_CLIENT -- Force Dynamic Client Registration when Obot authenticates to remote MCP servers, even when the authorization server supports Client ID Metadata Documents. Defaults to false.
  OBOT_SERVER_FORCE_DYNAMIC_CLIENT: false

  # config.OBOT_ARTIFACT_STORAGE_PROVIDER -- Storage provider for published workflows. Can be 's3', 'gcs', 'azure', or 'custom'. If unset, local disk storage is used.
  OBOT_ARTIFACT_STORAGE_PROVIDER: ""
  # config.OBOT_ARTIFACT_STORAGE_BUCKET -- Bucket or container name for published workflow storage.
  OBOT_ARTIFACT_STORAGE_BUCKET: ""
  # config.OBOT_ARTIFACT_S3_REGION -- Region for published workflow storage when using 's3' or 'custom'.
  OBOT_ARTIFACT_S3_REGION: ""
  # config.OBOT_ARTIFACT_S3_ENDPOINT -- Endpoint URL for published workflow storage when using 'custom' S3-compatible storage.
  OBOT_ARTIFACT_S3_ENDPOINT: ""
  # config.OBOT_ARTIFACT_AZURE_STORAGE_ACCOUNT -- Azure storage account name for published workflow storage.
  OBOT_ARTIFACT_AZURE_STORAGE_ACCOUNT: ""
  # config.OBOT_ARTIFACT_AZURE_TENANT_ID -- Azure tenant ID for published workflow storage when using explicit Azure credentials.
  OBOT_ARTIFACT_AZURE_TENANT_ID: ""
  # config.OBOT_ARTIFACT_AZURE_CLIENT_ID -- Azure client ID for published workflow storage when using explicit Azure credentials.
  OBOT_ARTIFACT_AZURE_CLIENT_ID: ""

  # config.OBOT_SERVER_MCPSERVER_SEARCH_IMAGE -- The container image to use for the MCP server search functionality.
  OBOT_SERVER_MCPSERVER_SEARCH_IMAGE: ""

  # config.OBOT_SERVER_MDM_ASSET_SOURCE -- URL to the MDM assets tar archive.
  OBOT_SERVER_MDM_ASSET_SOURCE: ""

  # config.OBOT_DEFAULT_SKILL_REPO_URL -- The default skill repository URL. Must be a full HTTPS GitHub URL. Defaults to https://github.com/obot-platform/skills. Only used on first-time setup (before the first owner user is created). Set to an empty string to disable.
  # OBOT_DEFAULT_SKILL_REPO_URL: ""
  # config.OBOT_DEFAULT_SKILL_REPO_REF -- The ref (branch, tag, or commit SHA) for the default skill repository. If empty, the repository's default branch is used. Only used on first-time setup.
  # OBOT_DEFAULT_SKILL_REPO_REF: ""

  # config.OBOT_DEFAULT_HOSTED_AGENTS_CATALOG_URL -- The default hosted agent catalog repository URL. Must be a full HTTPS URL. Defaults to https://github.com/obot-platform/hosted-agents-catalog. Only used on first-time setup (before the first owner user is created). Set to an empty string to disable.
  OBOT_DEFAULT_HOSTED_AGENTS_CATALOG_URL: ""
  # config.OBOT_DEFAULT_HOSTED_AGENTS_CATALOG_REF -- The ref (branch, tag, or commit SHA) for the default hosted agent catalog repository. If empty, the repository's default branch is used. Only used on first-time setup.
  OBOT_DEFAULT_HOSTED_AGENTS_CATALOG_REF: ""

# resources -- Resource requests and limits to use for Obot
resources: {}

# runtimeClassName -- RuntimeClass name for Obot server pods. This allows running Obot with specific container runtimes (e.g., gVisor, Kata).
runtimeClassName: ""

persistence:
  # persistence.enabled -- Enables persistence using a PVC mounted at /data.
  # This also persists local published workflow artifacts when OBOT_ARTIFACT_STORAGE_PROVIDER is unset,
  # because those files are stored under /data/.local/share/obot/published-artifacts.
  enabled: true
  # persistence.path -- The path the volume will be mounted
  path: /data
  # persistence.storageClass -- Persistent Volume storage class
  # If defined, storageClassName: <storageClass>
  # If set to "-", storageClassName: "", which disables dynamic provisioning
  # If undefined (the default) or set to null, no storageClassName spec is set, choosing the default provisioner
  storageClass: ""
  # persistence.accessModes -- Persistent Volume access modes.
  # ReadWriteOnce is appropriate for replicaCount=1.
  # ReadWriteMany is required for multi-replica deployments that rely on local artifact storage instead of object storage.
  accessModes:
    - ReadWriteOnce
  # persistence.size -- Persistent Volume size
  size: 8Gi
  existingClaim: ""

# extraVolumes -- A list of additional volumes to create
extraVolumes: []

# extraVolumeMounts -- A list of additional volume mounts to create
extraVolumeMounts: []

serviceAccount:
  # serviceAccount.create - Specifies whether a service account should be created
  create: true
  # serviceAccount.annotation - Annotations to add to the service account
  annotations: {}
  # serviceAccount.name - The name of the service account to use.
  # If not set and create is true, a name is generated using the fullname template
  name: ""

# mcpImagePullSecrets -- Configuration for creating image pull secrets for MCP containers.
# Each entry should contain registry credentials that will be used to create Kubernetes secrets.
# If you set .Values.secret.existingSecret, you must define `OBOT_SERVER_MCPIMAGE_PULL_SECRETS` to use these
mcpImagePullSecrets: []
# Example:
# mcpImagePullSecrets:
#   - name: "my-registry-secret"
#     registry: "my-registry.com"
#     username: "myuser"
#     password: "mypassword"
#     email: "myemail@example.com"

mcpNamespace:
  # mcpNamespace.name -- The namespace in which to deploy the MCP servers. Defaults to {{ .Release.Name }}-mcp
  name: ""
  # mcpNamespace.resourceQuota -- Define requests and limits for total resources available in the mcp namespace.
  resourceQuota: {}
    # resourceQuota:
    #   hard:
    #     requests.cpu: ""
    #     requests.memory: ""

  # mcpNamespace.annotation - Annotations to add to the mcp server namespace. Includes ArgoCD sync wave by default.
  annotations:
    argocd.argoproj.io/sync-wave: "-1"
  # mcpNamespace.networkPolicy -- Network policy configuration for the MCP namespace
  networkPolicy:
    # mcpNamespace.networkPolicy.enabled -- Enable network policy to restrict traffic to/from MCP servers. Default is true, and is recommended for production deployments.
    enabled: true
    # mcpNamespace.networkPolicy.dnsNamespace -- The namespace where DNS pods are running. Default is kube-system. Adjust if your cluster uses a different namespace for DNS services.
    dnsNamespace: kube-system
  # mcpNamespace.podSecurity -- Pod Security Admission configuration for the MCP namespace. Hosted agent sandboxes share this namespace, so the enforce level below is also applied to the security context of every sandbox pod. A restricted sandbox runs as UID 1000 and cannot escalate privileges, so a harness image that must run as root needs this lowered to baseline or privileged.
  podSecurity:
    # mcpNamespace.podSecurity.enabled -- Enable Pod Security Admission labels on the MCP namespace. Default is true.
    enabled: true
    # mcpNamespace.podSecurity.enforce -- Pod Security Standards level to enforce (privileged, baseline, or restricted). Default is restricted.
    enforce: restricted
    # mcpNamespace.podSecurity.enforceVersion -- Kubernetes version for the enforce policy. Default is latest.
    enforceVersion: latest
    # mcpNamespace.podSecurity.audit -- Pod Security Standards level to audit (privileged, baseline, or restricted). Default is restricted.
    audit: restricted
    # mcpNamespace.podSecurity.auditVersion -- Kubernetes version for the audit policy. Default is latest.
    auditVersion: latest
    # mcpNamespace.podSecurity.warn -- Pod Security Standards level to warn about (privileged, baseline, or restricted). Default is restricted.
    warn: restricted
    # mcpNamespace.podSecurity.warnVersion -- Kubernetes version for the warn policy. Default is latest.
    warnVersion: latest

# mcpServerDefaults -- Default Kubernetes configuration for all deployed MCP server pods
mcpServerDefaults:
  # mcpServerDefaults.affinity -- Affinity rules for MCP server pods (YAML format)
  # When set via Helm, these settings cannot be updated through the API
  affinity: {}
  # Example:
  # affinity:
  #   nodeAffinity:
  #     requiredDuringSchedulingIgnoredDuringExecution:
  #       nodeSelectorTerms:
  #       - matchExpressions:
  #         - key: workload-type
  #           operator: In
  #           values:
  #           - mcp
  #   podAntiAffinity:
  #     preferredDuringSchedulingIgnoredDuringExecution:
  #     - weight: 100
  #       podAffinityTerm:
  #         labelSelector:
  #           matchExpressions:
  #           - key: mcp-server-name
  #             operator: Exists
  #         topologyKey: kubernetes.io/hostname

  # mcpServerDefaults.tolerations -- Tolerations for MCP server pods (YAML list format)
  # When set via Helm, these settings cannot be updated through the API
  tolerations: []
  # Example:
  # tolerations:
  #   - key: "mcp-workload"
  #     operator: "Equal"
  #     value: "true"
  #     effect: "NoSchedule"
  #   - key: "node-role.kubernetes.io/mcp"
  #     operator: "Exists"
  #     effect: "NoSchedule"

  # mcpServerDefaults.resources -- Resource requests and limits for MCP server pods
  # When set via Helm, these settings cannot be updated through the API
  resources: {}
  # Example:
  # resources:
  #   requests:
  #     memory: "400Mi"
  #     cpu: "100m"
  #   limits:
  #     memory: "1Gi"
  #     cpu: "500m"

  # mcpServerDefaults.runtimeClassName -- RuntimeClass name for MCP server pods
  # This allows running MCP servers with specific container runtimes (e.g., gVisor, Kata)
  runtimeClassName: ""

  # mcpServerDefaults.storageClassName -- StorageClass name for nanobot workspace volumes
  storageClassName: ""

  # mcpServerDefaults.nanobotWorkspaceSize -- Size for nanobot workspace volumes (e.g., 1Gi)
  nanobotWorkspaceSize: ""

  # mcpServerDefaults.maxCPURequest -- Maximum CPU request allowed for MCP server pods
  # Also caps the built-in default CPU request when no explicit default is configured.
  maxCPURequest: ""

  # mcpServerDefaults.maxCPULimit -- Maximum CPU limit allowed for MCP server pods
  maxCPULimit: ""

  # mcpServerDefaults.maxMemoryRequest -- Maximum memory request allowed for MCP server pods
  # Also caps the built-in default memory request when no explicit default is configured.
  maxMemoryRequest: ""

  # mcpServerDefaults.maxMemoryLimit -- Maximum memory limit allowed for MCP server pods
  maxMemoryLimit: ""

# nodeSelector -- Configure node selector for pod assignment
nodeSelector: {}

# tolerations -- Configure tolerations for pod scheduling
tolerations: []
# Example:
# tolerations:
#   - key: "node-role.kubernetes.io/control-plane"
#     operator: "Exists"
#     effect: "NoSchedule"
#   - key: "dedicated"
#     operator: "Equal"
#     value: "obot"
#     effect: "NoSchedule"

# affinity -- Configure affinity rules for pod scheduling
affinity: {}
# Example:
# affinity:
#   nodeAffinity:
#     requiredDuringSchedulingIgnoredDuringExecution:
#       nodeSelectorTerms:
#       - matchExpressions:
#         - key: node-type
#           operator: In
#           values:
#           - compute
#   podAntiAffinity:
#     preferredDuringSchedulingIgnoredDuringExecution:
#     - weight: 100
#       podAffinityTerm:
#         labelSelector:
#           matchExpressions:
#           - key: app.kubernetes.io/name
#             operator: In
#             values:
#             - obot
#         topologyKey: kubernetes.io/hostname

# podDisruptionBudget -- PodDisruptionBudget configuration for Obot pods.
# Only enable this with replicaCount >= 2. With a single replica, minAvailable: 1
# permanently blocks node drains, since the one pod can never be evicted.
podDisruptionBudget:
  # podDisruptionBudget.enabled -- Enables creation of a PodDisruptionBudget. Disabled by default.
  enabled: false
  # podDisruptionBudget.minAvailable -- Minimum number of Obot pods that must remain available during voluntary disruptions. Ignored when maxUnavailable is set.
  minAvailable: 1
  # podDisruptionBudget.maxUnavailable -- Maximum number of Obot pods that may be unavailable during voluntary disruptions. Set this instead of minAvailable.
  maxUnavailable: ""
  # podDisruptionBudget.annotations -- Annotations to add to the PodDisruptionBudget.
  annotations: {}
