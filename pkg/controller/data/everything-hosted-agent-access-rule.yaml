apiVersion: obot.obot.ai/v1
kind: HostedAgentAccessRule
metadata:
  name: haar1-everything
  namespace: default
spec:
  manifest:
    displayName: Everything
    resources:
      - id: '*'
        type: selector
    subjects:
      - id: '*'
        type: selector
